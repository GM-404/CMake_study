#ifndef LONGESTCONSECUTIVE_HH
#define LONGESTCONSECUTIVE_HH
#include <vector>
#include <unordered_map>
/*128. 最长连续序列给定一个未排序的整数数组 nums，找到数字连续区间的最长长度。*/
class Solution_longestConsecutive
{
public:
    int longestConsecutive(std::vector<int> &nums);
};
#endif
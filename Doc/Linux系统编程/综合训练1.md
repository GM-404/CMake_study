```c
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <fcntl.h>
#include <sys/stat.h>
#include <sys/types.h>

#define BUFFER_SIZE 1024  // 定义缓冲区大小

int main(int argc, char *argv[]) {
    int fd_from, fd_to;    // 文件描述符
    ssize_t bytes_read;    // 读取的字节数
    char buffer[BUFFER_SIZE];  // 缓冲区

    // 检查命令行参数是否正确
    if (argc != 3) {
        fprintf(stderr, "用法: %s 源文件 目标文件\n", argv[0]);
        exit(EXIT_FAILURE);
    }

    // 打开源文件，只读模式
    fd_from = open(argv[1], O_RDONLY);
    if (fd_from == -1) {
        perror("打开源文件失败");
        exit(EXIT_FAILURE);
    }

    // 打开或创建目标文件，写入模式，如果文件存在则截断为0
    // 权限设置为所有者可读写，其他用户只读
    fd_to = open(argv[2], O_WRONLY | O_CREAT | O_TRUNC, S_IRUSR | S_IWUSR | S_IRGRP | S_IROTH);
    if (fd_to == -1) {
        perror("打开或创建目标文件失败");
        close(fd_from);  // 确保关闭已打开的文件
        exit(EXIT_FAILURE);
    }

    // 循环读取源文件内容并写入目标文件
    while ((bytes_read = read(fd_from, buffer, BUFFER_SIZE)) > 0) {
        if (write(fd_to, buffer, bytes_read) != bytes_read) {
            perror("写入文件失败");
            close(fd_from);
            close(fd_to);
            exit(EXIT_FAILURE);
        }
    }

    // 检查读取操作是否出错
    if (bytes_read == -1) {
        perror("读取文件失败");
        close(fd_from);
        close(fd_to);
        exit(EXIT_FAILURE);
    }

    // 关闭文件描述符
    if (close(fd_from) == -1) {
        perror("关闭源文件失败");
        exit(EXIT_FAILURE);
    }

    if (close(fd_to) == -1) {
        perror("关闭目标文件失败");
        exit(EXIT_FAILURE);
    }

    printf("文件复制成功: %s -> %s\n", argv[1], argv[2]);
    return 0;
}
```
## 1. argc 和 argv
argc 和 argv 是 C 语言中用于处理命令行参数的两个变量，在main函数中声明：

```c
int main(int argc, char *argv[])
```
argc：argument count 的缩写，表示命令行参数的数量，包括程序名本身
argv：argument vector 的缩写，是一个指向字符串的指针数组，每个元素指向一个命令行参数

示例：
当执行 
```c
./file_copy a.c b.c 时：
```
argc 的值为 3
argv[0] 指向 "./file_copy"（程序名）
argv[1] 指向 "a.c"（源文件）
argv[2] 指向 "b.c"（目标文件）

## 2. open 函数
open 函数用于打开或创建文件，原型如下：

```c
#include <fcntl.h>
int open(const char *pathname, int flags);
int open(const char *pathname, int flags, mode_t mode);
```
参数：
pathname：要打开的文件路径名
flags：打开文件的标志，常用标志包括：
- O_RDONLY：只读模式
- O_WRONLY：只写模式
- O_RDWR：读写模式
- O_CREAT：如果文件不存在则创建
- O_TRUNC：如果文件存在且以写入模式打开，则将其长度截断为 0
mode：当使用O_CREAT时，指定新文件的权限
返回值：
成功：返回文件描述符（非负整数）
失败：返回 - 1，并设置errno

## 3. read 函数
read 函数用于从文件描述符中读取数据，原型如下：

```c
#include <unistd.h>
ssize_t read(int fd, void *buf, size_t count);
```
参数：
fd：文件描述符
buf：指向缓冲区的指针，用于存储读取的数据
count：要读取的最大字节数
返回值：
成功：返回实际读取的字节数
到达文件末尾：返回 0
失败：返回 - 1，并设置errno

## 4. write 函数
write 函数用于向文件描述符写入数据，原型如下：

```c
#include <unistd.h>
ssize_t write(int fd, const void *buf, size_t count);
```
参数：
fd：文件描述符
buf：指向要写入数据的缓冲区
count：要写入的字节数
返回值：
成功：返回实际写入的字节数
失败：返回 - 1，并设置errno

注意：实际写入的字节数可能小于请求的字节数，这并不一定表示错误

## 5. close 函数
close 函数用于关闭文件描述符，原型如下：

```c
#include <unistd.h>
int close(int fd);
```
参数：fd 为要关闭的文件描述符
返回值：
成功：返回 0
失败：返回 - 1，并设置errno

注意：文件操作完成后应及时关闭文件描述符，以释放系统资源

## 6. 文件权限
在创建新文件时，mode 参数指定文件的访问权限，常用的权限常量包括：

S_IRUSR：所有者可读
S_IWUSR：所有者可写
S_IXUSR：所有者可执行
S_IRGRP：组用户可读
S_IWGRP：组用户可写
S_IXGRP：组用户可执行
S_IROTH：其他用户可读
S_IWOTH：其他用户可写
S_IXOTH：其他用户可执行

这些常量可以用位或运算符 | 组合使用。
程序编译与运行
编译程序：


```bash
gcc file_copy.c -o file_copy
```

运行程序，将 a.c 复制到 b.c：
```bash
./file_copy a.c b.c
```

验证结果：
```bash
diff a.c b.c  # 如果没有输出，表示两个文件内容完全相同
```
通过这个程序，我们学习了如何使用 Linux 系统调用进行文件操作，以及如何处理命令行参数，这些都是 Linux 系统编程的基础知识。

